import styled from 'styled-components'

const CustomTreeProvider = styled.div`
  .ant-tree-switcher {
    display: flex;
    align-items: center;
    justify-content: center;
  }
`

export const Styled = {
  CustomTreeProvider,
}
