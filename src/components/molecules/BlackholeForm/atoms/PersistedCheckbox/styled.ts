import styled from 'styled-components'
import { PossibleHiddenContainer } from '../PossibleHiddenContainer'

const PossibleHiddenContainerWidthPadding = styled(PossibleHiddenContainer)`
  padding-left: 4px;
`

export const Styled = {
  PossibleHiddenContainerWidthPadding,
}
