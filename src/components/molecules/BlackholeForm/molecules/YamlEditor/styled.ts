import styled from 'styled-components'

const BorderRadiusContainer = styled.div`
  height: 100%;

  .monaco-editor,
  .overflow-guard {
    border-radius: 8px;
  }
`

export const Styled = {
  BorderRadiusContainer,
}
