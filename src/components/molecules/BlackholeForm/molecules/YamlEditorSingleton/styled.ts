import styled from 'styled-components'

const BorderRadiusContainer = styled.div`
  min-height: 75vh;

  .monaco-editor,
  .overflow-guard {
    border-radius: 8px;
  }
`

export const Styled = {
  BorderRadiusContainer,
}
